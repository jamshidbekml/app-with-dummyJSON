import { Typography } from '@mui/material';

const NotFound = () => {
    return (
        <Typography align="center" variant="h2" style={{ marginTop: '15%' }}>
            404 | Page not found.
        </Typography>
    );
};

export default NotFound;
